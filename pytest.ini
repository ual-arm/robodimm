[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show short test summary for all failures/errors/warnings
addopts = -v --tb=short -q

# Custom markers
markers =
    pinocchio: Tests requiring the Pinocchio and Pink libraries (conda environment)

# Logging
log_cli = false
